import { AppShell } from "@/layouts/AppShell";

/** See StudentLayout.tsx for why this file exists despite delegating entirely to AppShell today. */
export function SuperAdminLayout() {
  return <AppShell />;
}
