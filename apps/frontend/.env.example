# Base URL for the RyuZen backend API.
# Matches the backend's default PORT (see backend config/env.ts) plus the
# /api/v1 prefix every backend route is mounted under.
VITE_API_BASE_URL=http://localhost:5000/api/v1
