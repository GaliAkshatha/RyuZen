# Base URL for the RyuZen backend API - matches the backend's real
# mount point (/api/v1), confirmed against the existing frontend's
# working configuration.
VITE_API_BASE_URL=http://localhost:5000/api/v1
